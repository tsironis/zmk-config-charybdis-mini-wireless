manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: tsironis
      url-base: https://github.com/tsironis

  projects:
    - name: zmk
      remote: tsironis
      revision: charybdis-sleep-debug  # charybdis-input-fix + sleep-entry breadcrumbs (SLEEP-DEBUG branch)
      import: app/west.yml
    - name: zmk-pmw3610-driver
      remote: tsironis
      # EXPERIMENT 15: b8ecaea with b447408 ("Fix wake warp") reverted, i.e. the
      # driver is NOT registered with Zephyr device PM. Isolates our PM/IRQ sleep
      # path with the trackball still present. Revert to `main` after the run.
      revision: 0df27a9cd628848437e17965e3f662f6255f7be0

  self:
    path: config
